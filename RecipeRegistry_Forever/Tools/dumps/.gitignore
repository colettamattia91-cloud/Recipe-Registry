# Gli ingressi del database restano fuori da questo repo, che e' pubblico.
#
# Il file che conta e' Data/Metadata/RecipeMetadata_Generated.lua, ed e'
# versionato lui. Cio' da cui nasce -- le catture del Collector e il bundle del
# datamining -- si archivia nel repo privato accanto a questo,
# ../WowForeverMining/data: una cattura si rifa' solo giocando, un giro dai
# trainer un mestiere per volta, e va conservata, ma non qui.
#
# Questa cartella resta come spazio di lavoro: mining.lua convertito da
# convert-mining.ps1, i file temporanei di import-dumps.ps1, e l'archivio locale
# per chi il repo privato non ce l'ha.
*
!.gitignore
